# Rollback note: level selection repair

This note records that the lesson-first and year-level sprint diagnostic changes were rolled back after level selection stopped opening in the student flow.

The next implementation should be rebuilt in a feature branch with small commits and browser-tested after each change.
